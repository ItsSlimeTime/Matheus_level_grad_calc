# Level & Gradient Calculator

A single-page level and gradient calculator for architecture projects (ramp flights, landings, end levels).

**Live app:** https://itsslimetime.github.io/Matheus_level_grad_calc/

Everything lives in `index.html`, with no build step or dependencies. To update the app, replace `index.html` and push to `main`. GitHub Pages redeploys automatically.
